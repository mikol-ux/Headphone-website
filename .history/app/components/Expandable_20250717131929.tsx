import Expandable from "@/components/animata/carousel";
import React from "react";

const Expandabe = () => {
	return <Expandable className="w-full min-w-72 h-screen storybook-fix" />;
};

export default Expandabe;
