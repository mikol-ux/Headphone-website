import Expandable from "@/components/animata/carousel";
import React from "react";

const Expandabe = () => {
	return <Expandable className="w-full min-w-72 h-[70vh] storybook-fix" />;
};

export default Expandabe;
